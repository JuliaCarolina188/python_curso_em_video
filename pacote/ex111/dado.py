from ex111.moeda import dobro, metade, aumentar, diminuir, moeda, resumo
numero = float(input('Digite um número: '))
taxa_mai = float(input('Digite uma taxa para ser adicionada: '))
taxa_dim = float(input('Digite uma taxa para ser diminuída: '))
resumo(numero, taxa_mai, taxa_dim)
